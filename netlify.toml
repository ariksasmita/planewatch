[build]
  command = "npm run build"
  publish = ".output/public"

[build.environment]
  NITRO_PRESET = "netlify"

# Required runtime environment variable in Netlify UI:
# NUXT_AERODATABOX_RAPID_API_KEY=your_rapidapi_key_here
